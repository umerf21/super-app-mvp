import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: { flex: 1, padding: 16 },
  noteItem: {
    padding: 12,
    backgroundColor: '#4F8EF7',
    borderRadius: 8,
    marginBottom: 12,
  },
  noteTitle: { color: '#fff', fontWeight: 'bold' },
  addButton: {
    padding: 16,
    backgroundColor: '#4F8EF7',
    borderRadius: 8,
    alignItems: 'center',
    marginTop: 12,
  },
  addText: { color: '#fff', fontWeight: 'bold' },
});